 I have:

  free -m
                 total        used        free      shared  buff/cache   available
  Mem:             954         372         165           4         585         581
  Swap:              0           0           0

  And VPS on Ubuntu 24.04 mini 1cpu, 1 gb, 47 Gb disk.
  ---
  Reminde me the best way to deploy my `phrasal` application on this VPS so that it would be enough.

● With 1 GB RAM, Docker is off the table — the daemon alone eats ~100 MB before any containers start. Here's the plan:

  Native deployment (no Docker)

  RAM budget (~950 MB total):

 With 1 GB RAM, Docker is off the table — the daemon alone eats ~100 MB before any containers start. Here's the plan:

  Native deployment (no Docker)

  RAM budget (~950 MB total):

  ┌─────────────────────────┬───────────────┐
  │        Component        │ Estimated RAM │
  ├─────────────────────────┼───────────────┤
  │ OS + systemd            │ ~200 MB       │
  ├─────────────────────────┼───────────────┤
  │ PostgreSQL              │ ~100 MB       │
  ├─────────────────────────┼───────────────┤
  │ Spring Boot (tuned JVM) │ ~350 MB       │
  ├─────────────────────────┼───────────────┤
  │ Nginx (static React)    │ ~10 MB        │
  ├─────────────────────────┼───────────────┤
  │ Swap buffer             │ ~290 MB       │
  └─────────────────────────┴───────────────┘

  # Step 0 — Add swap (critical with 1 GB)
  sudo fallocate -l 2G /swapfile
  sudo chmod 600 /swapfile
  sudo mkswap /swapfile
  sudo swapon /swapfile
  echo '/swapfile none swap sw 0 0' | sudo tee -a /etc/fstab

  # Step 1 — Install PostgreSQL
  sudo apt update && sudo apt install -y postgresql postgresql-client
  sudo -u postgres psql -c "CREATE USER phrasaluser WITH PASSWORD 'phrasalpass';"
  sudo -u postgres psql -c "CREATE DATABASE phrasaldb OWNER phrasal;"

  # Step 2 — Install Java 21 + Nginx
  sudo apt install -y openjdk-21-jre-headless nginx

  # Step 3 — Build & deploy backend
  #Build on your local machine (the VPS doesn't have enough RAM to compile):
  # LOCAL machine
  cd backend && mvn clean package -DskipTests
  # scp target/backend-*.jar ubuntu@<VPS_IP>:/opt/phrasal/app.jar
  ssh -i ~/dev/scripts/vps/ssh-key-2026-06-26.key ubuntu@141.144.227.73 "sudo mkdir -p /opt/phrasal && sudo chown ubuntu:
  /opt/phrasal"

  scp -i ~/dev/scripts/vps/ssh-key-2026-06-26.key \
  /home/ubuntuu/dev/mine/phrasal/backend/target/backend-*.jar \
    ubuntu@141.144.227.73:/opt/phrasal/app.jar

  # Create systemd service /etc/systemd/system/phrasal.service:
  sudo bash -c 'cat << "EOF" > /etc/systemd/system/phrasal.service
  [Unit]
  Description=Phrasal Backend
  After=postgresql.service
  Requires=postgresql.service

  [Service]
  User=phrasal
  ExecStart=/usr/bin/java \
    -Xms128m -Xmx256m \
    -XX:+UseSerialGC \
    -XX:MaxMetaspaceSize=128m \
    -jar /opt/phrasal/app.jar \
    --spring.datasource.url=jdbc:postgresql://localhost:5432/phrasaldb \
    --spring.datasource.username=phrasaluser \
    --spring.datasource.password=phrasalpass
  Restart=always

  [Install]
  WantedBy=multi-user.target
  EOF'

  sudo useradd -r -s /bin/false phrasal
  sudo mkdir -p /opt/phrasal && sudo chown phrasal: /opt/phrasal
  sudo systemctl enable --now phrasal

  # Step 4 — Deploy frontend with Nginx
  # Build locally, copy dist:
  # LOCAL machine
  cd frontend && npm run build

  # On VPS create
  mkdir -p /var/www/phrasal

  ssh -i ~/dev/scripts/vps/ssh-key-2026-06-26.key \
  ubuntu@141.144.227.73 "sudo chown ubuntu: \
  /var/www/phrasal"

  scp -i ~/dev/scripts/vps/ssh-key-2026-06-26.key \
    -r /home/ubuntuu/dev/mine/phrasal/frontend/dist/* \
      ubuntu@141.144.227.73:/var/www/phrasal/

  # Nginx config /etc/nginx/sites-available/phrasal:
  sudo bash -c 'cat << "EOF" > /etc/nginx/sites-available/phrasal
  server {
      listen 80;
      server_name your-domain.com;
      # server_name phrasal.ddns.net;

      root /var/www/phrasal;
      index index.html;

      location / {
          try_files $uri $uri/ /index.html;
      }

      location /api/ {
          proxy_pass http://127.0.0.1:8080;
          proxy_set_header Host $host;
          proxy_set_header X-Real-IP $remote_addr;
      }
  }
  EOF'

  sudo ln -s /etc/nginx/sites-available/phrasal /etc/nginx/sites-enabled/
  sudo rm /etc/nginx/sites-enabled/default
  sudo nginx -t && sudo systemctl reload nginx

  #The key JVM flags are -Xmx256m and -XX:+UseSerialGC — SerialGC has the lowest memory overhead. The 2 GB swap gives you
  #headroom so the OOM killer doesn't nuke your app. Want me to check the actual Spring profiles and datasource config to
  #make these commands copy-paste ready?

----
 Run this on VPS to install certbot and get a free Let's Encrypt certificate:

   ssh -i ~/dev/scripts/vps/ssh-key-2026-06-26.key ubuntu@141.144.227.73 \
     "sudo apt install -y certbot python3-certbot-nginx && sudo certbot --nginx -d phrasal.ddns.net"

   It will ask for your email (for renewal notices) and to agree to terms. Certbot auto-configures nginx to redirect HTTP →
   HTTPS.

   One thing first — make sure port 443 is open on both:
   1. OCI Security List — add ingress rule for TCP 443
   2. iptables on VPS:
   ssh -i ~/dev/scripts/vps/ssh-key-2026-06-26.key ubuntu@141.144.227.73 \
     "sudo iptables -I INPUT 5 -p tcp --dport 443 -j ACCEPT && sudo netfilter-persistent save"

   Open port 443 first, then run certbot.

---
 iptables:
 >sudo iptables -L INPUT -n --line-numbers # see all
 >sudo iptables -I INPUT 5 -p tcp --dport 443 -j ACCEPT && sudo netfilter-persistent save # accept 443
 >sudo iptables -I INPUT 5 -p tcp --dport 8080 -j ACCEPT && sudo netfilter-persistent save # open 8080
 >sudo iptables -D INPUT -p tcp --dport 8080 -j ACCEPT && sudo netfilter-persistent save # close 8080
 ---
 tail log:
 >journalctl -u phrasal -f

---
  Step-by-step (Releasing)

  1. Make sure fullstack branch is clean and pushed
  git status
  git push origin fullstack
  2. Create the annotated tag
  git tag -a v0.8.0 -m "v0.8.0

  Full-Stack Monorepo Migration

  Architecture:
  - Migrated from frontend-only SPA to full-stack monorepo (backend/frontend/e2e)
  - Spring Boot 3.4.1 REST API serving verb data from PostgreSQL
  - Verb list and detail pages now powered by backend API
  - Kubernetes manifests and Skaffold config for minikube deployment

  Features:
  - Fuzzy search with Fuse.js in search modals
  - Keyboard navigation and accessibility for quizzes and modals
  - VPS deploy script with interactive menu and tab completion

  Code Quality:
  - Google Checkstyle enforced across backend (all violations resolved)
  - Lombok introduced across backend module
  - Clean-code and Spring Boot patterns refactoring
  - Tailwind CSS v3 to v4 migration

  Performance:
  - Test files consolidated (869 to 71) with multi-threading
  - Switched to happy-dom and split list page tests (73s to 16s)

  Infrastructure:
  - Node upgraded to 26.3.1
  - Nginx proxy timeout set to 30s
  - Playwright browser install added to CI e2e job
  - Frontend deploy switched from rsync to scp"
  3. Push the tag
  git push origin v0.8.0
  4. Create the GitHub Release (optional, gives you a nice UI page)
  gh release create v0.8.0 --title "v0.8.0 — Full-Stack Monorepo Migration" --notes "..."
