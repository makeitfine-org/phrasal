{% for old_domain in redirect_domains %}
server {
    listen 80;
    server_name {{ old_domain }};

    root /var/www/phrasal-construction;
    index index.html;

    location / {
        try_files $uri /index.html;
    }
}
{% endfor %}
