apiVersion: v1
kind: Service
metadata:
  name: phrasal-frontend
  labels:
    app: phrasal-frontend
spec:
  type: LoadBalancer
  selector:
    app: phrasal-frontend
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 80
      name: http
