apiVersion: v1
kind: Secret
metadata:
  name: app-secrets
  labels:
    app: backend
type: Opaque
stringData:
  database.username: postgres
  database.password: postgres

# todo:
# One caveat: your DB_USERNAME and DB_PASSWORD GitHub secrets still need to be set to real production credentials. From
# the previous session, they were flagged as still using postgres/postgres — so that's worth updating if you haven't
# already.
